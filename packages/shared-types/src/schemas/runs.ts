// Here you go, look at all of these:

// @/packages/shared-types/src/types/workflows.ts 

// Let's actually move the run detail schemas to src/schemas/runs and schemas/api/runs. 

// And then derive the types of them in the mirror types package respectfully.

// Then we can use this type in our component